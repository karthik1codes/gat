# Google OAuth Client ID (same as frontend VITE_GOOGLE_CLIENT_ID)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Optional: encrypt per-user SSE keys (min 32 chars)
# GAT_SERVER_SECRET=your-32-char-secret-here

# Optional: JWT signing secret
# GAT_JWT_SECRET=your-jwt-secret

# Optional: SQLite path
# GAT_DATABASE_URL=sqlite:///./data/gat.db
